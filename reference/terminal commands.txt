// first time after cloning the main repo
git switch -c "BRANCH_NAME"


// to access your branch
git switch "BRANCH_NAME"


// after adding or modifying code
git add *
git fetch origin
get merge origin/master
get push

// pull from terminal or webpage
git pull
